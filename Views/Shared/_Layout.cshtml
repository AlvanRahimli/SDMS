<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="#">SDMS - Student Database Management System</a>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" asp-action="">Home <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a asp-action="Choose" asp-controller="Students"></a>
                </li>
            </ul>
            <a asp-action="Register" asp-controller="Auth" class="nav-link">Register</a>
            <a asp-action="GetLoginStudent" asp-controller="Auth" class="nav-link">Login Student</a>
            <a asp-action="GetLoginTeacher" asp-controller="Auth" class="nav-link">Login Teacher</a>
            <a asp-action="LogOut" asp-controller="Auth" class="nav-link">Log Out</a>
        </div>
    </nav>
    <br>
    @RenderBody()
</body>
</html>